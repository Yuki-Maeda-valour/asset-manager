### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Asset {
  borrowings: [Borrowing]
  createdAt: String
  id: Int
  name: String
  status: AssetStatus
  type: AssetType
  updatedAt: String
}

enum AssetStatus {
  AVAILABLE
  LENT
  RESERVED
  SUSPENDED
}

enum AssetType {
  MONITOR
  PC
  SP
  WIFI
}

type Borrowing {
  asset: Asset
  assetId: Int
  borrowedAt: String
  createdAt: String
  deadline: String
  id: Int
  returnedAt: String
  updatedAt: String
  user: User
  userId: Int
}

type Mutation {
  createAsset(name: String, status: AssetStatus, type: AssetType): Asset
  createBorrowing(assetId: Int, borrowedAt: String, deadline: String, userId: Int): Borrowing
  createUser(role: Role, username: String): User
  deleteAsset(id: Int): Asset
  deleteBorrowing(id: Int): Borrowing
  deleteUser(id: Int): User
  updateAsset(id: Int, name: String, status: AssetStatus, type: AssetType): Asset
  updateBorrowing(assetId: Int, borrowedAt: String, deadline: String, id: Int, userId: Int): Borrowing
  updateUser(id: Int, role: Role, username: String): User
}

type Query {
  asset(id: Int): Asset
  assets: [Asset]
  borrowing(id: Int): Borrowing
  borrowings: [Borrowing]
  user(id: Int): User
  users: [User]
}

enum Role {
  ADMIN
  USER
}

type User {
  borrowings: [Borrowing]
  createdAt: String
  id: Int
  role: Role
  updatedAt: String
  username: String
}